package main

/*

引数を小文字に
普通に引数を改行
マクロ
crrl-s
crrl-j

*/

type StrctExample struct {
	A string
	B string
	C string
	D string
	E string
	F string
	G string
	H string
	I string
	J string
	K string
	L string
	M string
	N string
	O string
	P string
	Q string
	R string
	S string
	T string
	U string
	V string
	W string
	X string
	Y string
	Z string
}

func NewStructExample(A string, B string, C string, D string, E string, F string, G string, H string, I string, J string, K string, L string, M string, N string, O string, P string, Q string, R string, S string, T string, U string, V string, W string, X string, Y string, Z string) *StrctExample {
	return &StrctExample{
		A: a,
		B: b,
		C: c,
		D: d,
		E: e,
		F: f,
		G: g,
		H: h,
		I: i,
		J: j,
		K: k,
		L: l,
		M: m,
		N: n,
		O: o,
		P: p,
		Q: q,
		R: r,
		S: s,
		T: t,
		U: u,
		V: v,
		W: w,
		X: x,
		Y: y,
		Z: z,
	}
}
